{
  "name": "cz-migration-auth-mock",
  "private": true,
  "version": "1.0.0",
  "description": "ALB + Okta OIDC mock server for CZ migration development",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "supertest": "^7.0.0"
  }
}
